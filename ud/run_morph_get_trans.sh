for RANDOM_SEED in 54360 44184 20423 80520 27916; do #63795 30528 78160 12345 95148; do
  NUMPY_SEED=`echo $RANDOM_SEED | cut -c1-4`
  PYTORCH_SEED=`echo $RANDOM_SEED | cut -c1-3`
  CUDA_DEVICE=$1
  TRANSFORMER_NAME=$2
  TRANSFORMER_MODEL=$3
  
  export CUDA_DEVICE=${CUDA_DEVICE}
  export TRANSFORMER_MODEL=${TRANSFORMER_MODEL}
  export RANDOM_SEED=${RANDOM_SEED}
  export NUMPY_SEED=${NUMPY_SEED}
  export PYTORCH_SEED=${PYTORCH_SEED}
  
  allennlp train mbert_crf_morph_get_trans.jsonnet -s ud/output/models_alephbert/${TRANSFORMER_NAME}/morph_${RANDOM_SEED}
done
